import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'weusthem-developer-test-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
